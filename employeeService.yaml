server:
  port: ${EMPLOYEE_SERVICE_PORT:8105}
       
management:
  endpoints:
    web:
      exposure:
        include: "*"    
        
database_host: ${MONGO_HOST:localhost}
database_port: ${MONGO_PORT:27017}
database_name: ${EMPLOYEE_DATABASE:employee_db}

random_value: abc
    
    
  
